schema {
    query: Query,
    mutation: Mutation,
    subscription: Subscription
}

type Query {
    nodes(nodeType: String!): [Node]
}

type Node {
    property(name: String): String,
    children(nodeType: String!): [Node]
}

type Mutation {

}

type Subscription{

}